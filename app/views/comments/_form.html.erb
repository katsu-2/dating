
<%= form_with(model: [@post, @post.comments.build],
local: true) do |f| %>
  <div class="card-body">
    <%= f.text_area :content, placeholder: "コメントをする" %>
  </div>
  <div class="actions">
    <%= f.submit "コメント" %>
  </div>
<% end %>
